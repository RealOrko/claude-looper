/**
 * LogsPanel Icons
 */
import { Info, CheckCircle2, AlertTriangle, XCircle, Terminal, Hash } from 'lucide-react';

export const levelIconComponents = {
  info: Info,
  success: CheckCircle2,
  warning: AlertTriangle,
  error: XCircle,
  output: Terminal,
  debug: Hash,
};
