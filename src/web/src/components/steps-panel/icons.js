/**
 * StepsPanel Icons
 */
import { CheckCircle2, XCircle, AlertTriangle, PlayCircle, Circle } from 'lucide-react';

export const statusIcons = {
  completed: CheckCircle2,
  failed: XCircle,
  blocked: AlertTriangle,
  in_progress: PlayCircle,
  pending: Circle,
};
